# Database Configuration
DBT_HOST=localhost
DBT_USER=root
DBT_PASSWORD=<use-secure-password>
DBT_DBNAME=analytics
DBT_SCHEMA_DEV=staging
DBT_SCHEMA_PROD=mart

# Airflow/Docker Configuration
AIRFLOW_UID=1000

# Airbyte Configuration - STORE THESE SECURELY (use Airflow Connections instead)
# DO NOT commit credentials to git
AIRBYTE_API_URL=http://localhost:8000
# Credentials managed via Airflow Connections UI, not .env
